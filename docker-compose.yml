services:
  nexus:
    build: .
    container_name: nexus-api
    restart: unless-stopped
    ports:
      - "8099:8099"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./logs:/app/logs

